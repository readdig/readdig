services:
  app:
    container_name: app
    restart: always
    build:
      context: .
      dockerfile: Dockerfile
    env_file:
      - .env
    image: app
    ports:
      - '80:80'
    networks:
      - frontend
      - backend
    logging:
      driver: 'json-file'
      options:
        max-size: '50MB'
        max-file: '10'
networks:
  frontend: 
    name: frontend
    driver: bridge
  backend: 
    name: backend
    driver: bridge
    external: true